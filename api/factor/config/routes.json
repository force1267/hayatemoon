{
  "routes": [
    {
      "method": "GET",
      "path": "/factors",
      "handler": "factor.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/factors/count",
      "handler": "factor.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/factors/:id",
      "handler": "factor.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/factors",
      "handler": "factor.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/factors/:id",
      "handler": "factor.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/factors/:id",
      "handler": "factor.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
