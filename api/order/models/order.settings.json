{
  "kind": "collectionType",
  "collectionName": "orders",
  "info": {
    "name": "order"
  },
  "options": {
    "increments": true,
    "timestamps": true
  },
  "attributes": {
    "price": {
      "type": "integer"
    },
    "restaurant": {
      "model": "restaurant",
      "via": "orders"
    },
    "state": {
      "type": "enumeration",
      "enum": [
        "incart",
        "notpaid",
        "pending",
        "delivering",
        "delivered"
      ],
      "default": "incart"
    },
    "dishes": {
      "collection": "dish"
    },
    "user": {
      "via": "orders",
      "plugin": "users-permissions",
      "model": "user"
    }
  }
}
